# Per-developer overrides for justfile variables. Copy to `.env` and edit.
# `.env` is gitignored; `.env.example` is checked in as documentation.

# IP address of the DE10-Nano on your network. Used by `deploy-*` and
# `probe-menu-core` recipes.
MISTER_IP=192.168.1.79
